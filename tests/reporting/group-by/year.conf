basedir=group-by/

input.txn.dir = "txns"
input.txn.glob = "**plain-group-by.txn"

reporting.reports = ["balance", "balance-group"]
reporting.accounts = ["^a:.*", "^e:.*"]

reports.balance-group.group-by = "year"

include "group-by-accounts.conf"
