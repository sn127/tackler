basedir=ok/

input.fs.dir = "txns"
input.fs.glob = "**.txn"

reporting.reports = ["balance", "balance-group","register", "equity", "identity"]
reporting.accounts = ["do-not-match-anything"]

include "ok-accounts.conf"
