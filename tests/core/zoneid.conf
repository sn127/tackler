tackler {
  core {
    basedir = ok/

    timezone = "+02:00"

    input.fs.dir = "not-found/"
    input.fs.glob = "**.not-found"

    reporting.reports = []
    reporting.exports = ["identity"]

    include "ok-accounts.conf"
  }
}
