basedir=.

input {

  storage = git

  git {
    repository = "not-git-repo"
    ref = "master"
    dir = "git-txns"
    suffix = ".txn"
  }
}

reporting.reports = ["identity"]

accounts.strict = false
