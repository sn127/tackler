tackler {
  core {
    basedir = ok/

    input.fs.dir = "txns/"
    input.fs.glob = "**.txn"

    reporting {
      reports = ["balance", "balance-group", "register"]
      console = true
    }
  
    include "ok-accounts.conf"
  }
}
