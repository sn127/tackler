tackler {
  core {
    basedir = ok/

    input.fs.dir = "not-found/"
    input.fs.glob = "**.not-found"

    reporting.reports = ["balance", "balance-group", "register"]
    reporting.exports = ["identity", "equity"]

    include "ok-accounts.conf"
  }
}
