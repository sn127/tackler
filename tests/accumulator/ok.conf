basedir=ok/

input.txn.dir = "../txns/"
input.txn.glob = "**.txn"

reporting.reports = [ "balance", "balance-group", "register", "equity" ]
reports.balance-group.group-by = "date"

include "ok-accounts.conf"

